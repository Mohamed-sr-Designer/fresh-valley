/* =====================================================================
   FRESH VALLEY — Published store content
   Written by the admin's "Publish" button (GitHub) or downloaded from the
   admin and committed by hand. null = the theme defaults in theme.js.
   Shape: { updatedAt, theme: {...}, catalog: {...}, settings: {...} }
   ===================================================================== */
window.FV_CONTENT = null;
